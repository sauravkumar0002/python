#include<iostream>
using namespace std;
 int main(){
    int n;
    cout<<"Enter a number:\n";
    cin>>n;
    (n%2==0) ? cout<<"Even number\n" : cout<<"Odd number\n";
    // The ternary operator checks if n is even or odd and prints the result accordingly.


 }